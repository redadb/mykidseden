'use client'; import { useEffect } from 'react'; export default function RolesError({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) { useEffect(() => { console.error('Roles Page Error:', error); }, [error]); return ( <div className="error-container"><h2>Something went wrong in Roles</h2><p>{error.message}</p><button onClick={() => reset()} className="error-reset-button">Try again</button></div> ); }
