'use client'; import { useEffect } from 'react'; export default function RoadmapError({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) { useEffect(() => { console.error('Roadmap Page Error:', error); }, [error]); return ( <div className="error-container"><h2>Something went wrong in Roadmap</h2><p>{error.message}</p><button onClick={() => reset()} className="error-reset-button">Try again</button></div> ); }
